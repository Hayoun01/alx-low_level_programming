#include "main.h"

/**
 * print_sig - Checks sign of a num.
 * @n: take number.
 * Return: 1 if is alpha 0 otherwise
 */
int print_sign(int n)
{
	if (c > 0)
		_putchar('+');
		return (1);
	else if (c < 0)
		_putchar('-');
		return (-1);
	_putchar('0')
	return (0);
}
